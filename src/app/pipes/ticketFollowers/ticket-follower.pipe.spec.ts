import { TicketFollowerPipe } from './ticket-follower.pipe';

describe('TicketFollowerPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketFollowerPipe();
    expect(pipe).toBeTruthy();
  });
});
