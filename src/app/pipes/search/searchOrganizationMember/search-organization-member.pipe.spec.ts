import { SearchOrganizationMemberPipe } from './search-organization-member.pipe';

describe('SearchOrganizationMemberPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOrganizationMemberPipe();
    expect(pipe).toBeTruthy();
  });
});
