import { SearchAreaPipe } from './search-area.pipe';

describe('SearchAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
