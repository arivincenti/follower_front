import { SearchNotificationPipe } from './search-notification.pipe';

describe('SearchNotificationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNotificationPipe();
    expect(pipe).toBeTruthy();
  });
});
